export const size = {
    phone: '375px',
    tablet: '768px',
};

export const device = {
    phone: `(max-width: ${size.phone})`,
    tablet: `(max-width: ${size.tablet})`,
};

export default { size, device };
